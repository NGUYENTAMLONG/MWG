import {
  swaggerSchemaArr,
  swaggerSchemaExample,
} from 'src/common/utils/swagger.util';

export const EXAM_CONST = {
  MODEL_NAME: 'exam',
  MODEL_PROVIDER: 'EXAM_MODEL',
};

export const EXCEPTION_EXAM = {};

export const EXAM_SWAGGER_RESPONSE = {};
